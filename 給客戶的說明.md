# Ansys License 連線診斷工具　操作說明

虎門科技股份有限公司　Taiwan Auto-Design Co.
技術支援：cae-support@cadmen.com

---

## 這是什麼

當 Ansys 軟體出現「無法連上授權伺服器」之類的訊息時，這支工具會自動檢查貴司環境，
找出造成連線失敗的原因，並產生一份報告。

---

## 這支工具會做什麼、不會做什麼

| | |
| --- | --- |
| **會做** | 讀取本機的 Ansys 授權設定、測試與授權伺服器的連線、檢查授權服務狀態 |
| **會做** | 產生一份報告檔（HTML 與 TXT 各一份）放在工具旁的 `reports` 資料夾 |
| **不會做** | **不會修改貴司任何設定。本工具全程唯讀。** |
| **不會做** | 不會自動連線外網，不會回傳任何資料。報告要不要寄出，由貴司決定 |
| **不會做** | 不會安裝任何東西 |

偵測到問題時，工具會把**修復指令列出來**，由貴司 IT 自行判斷是否執行。工具本身不會動手。

---

## 需要準備什麼

不需要安裝任何東西。只要是 Windows 10 或 Windows 11 即可（使用系統內建的 PowerShell）。

---

## 操作步驟

### 1. 解壓縮

把收到的壓縮檔解壓縮到任一資料夾，例如桌面。請保持 `執行診斷.bat` 與
`Check-AnsysLicense.ps1` 在**同一個資料夾**內。

### 2. 執行

在 `執行診斷.bat` 上按**滑鼠右鍵**，選擇**以系統管理員身分執行**。

> 一般使用者權限也可以執行，但部分資訊會查不到（例如是哪一支程式占用了授權連接埠）。
> 建議用系統管理員身分執行，診斷會比較完整。

### 3. 等待完成

畫面會依序顯示檢查進度，通常在一分鐘內完成。
若授權伺服器沒有回應，可能需要等待數分鐘（等待逾時）。

### 4. 查看結果與回傳

畫面最後會列出診斷結果，並顯示報告檔的位置。

請將 `reports` 資料夾內產生的檔案，以電子郵件寄至 **cae-support@cadmen.com**，
並在信中註明畫面上顯示的**案件編號**。

> `.html` 與 `.txt` 兩份內容完全相同，寄任何一份都可以。
> 若貴司郵件系統會阻擋 `.html` 附件，請改寄 `.txt`，或直接把 `.txt` 的內容貼進信件內文。

---

## 診斷結果怎麼看

結果分成三個等級：

| 等級 | 意義 |
| --- | --- |
| **【確定】** | 已確認的根本原因，依照建議處理即可 |
| **【可疑】** | 發現異常，但無法確定是不是主因，需要一併參考 |
| **【需人工】** | 資料不足或情況特殊，請將報告回傳由我們判讀 |

工具只在證據充分時才會標示【確定】，不會臆測。

---

## 兩台機器都要跑

Ansys 授權架構分成**授權伺服器**與**使用者電腦**兩端，問題可能出在任一端。

- 如果**全公司都不能用** → 請優先在**授權伺服器**上執行
- 如果**只有某幾台不能用** → 請在**不能用的那台電腦**上執行
- 不確定的話，**兩邊都跑一次**，效果最好

兩邊執行時請使用**相同的案件編號**，我們才能對照判讀。

要指定案件編號時，請開啟命令提示字元，切換到工具所在的資料夾後執行（**前面的 `.\` 不能省略**）：

```
.\執行診斷.bat -CaseId 貴司名稱-20260828
```

> 若我們寄給您的壓縮檔中含有 `expected.json`，案件編號已經寫在裡面，
> 直接雙擊 `執行診斷.bat` 即可，不需要輸入任何參數。

---

## 資料隱私

報告中會包含：本機電腦名稱、作業系統版本、Ansys 安裝路徑與版次、授權伺服器主機名稱與
連接埠、授權服務狀態與使用數量、License 檔案的 SERVER 行。

**報告不會包含授權碼，也不會包含 License 檔案的 INCREMENT 內容。**

報告的第一頁就會列出該份報告實際包含哪些資訊，請貴司確認後再決定是否寄出。

若貴司資安規範較嚴格，可以加上 `-Anonymize` 參數執行，工具會把使用者帳號、內網 IP、
第三方軟體名稱雜湊處理後才寫入報告：

```
.\執行診斷.bat -Anonymize
```

---

## 如果無法執行

部分企業的資安軟體會阻擋批次檔或 PowerShell 腳本。若雙擊後沒有反應或出現錯誤，
請開啟 PowerShell 視窗，切換到工具所在的資料夾後，手動執行：

```powershell
powershell -ExecutionPolicy Bypass -File .\Check-AnsysLicense.ps1
```

仍然無法執行的話，請與我們聯絡：**cae-support@cadmen.com**

---

## 常見問題

**Q：這支工具會不會把資料傳到外面？**
A：不會。工具完全在本機運作，只會產生檔案。要不要寄出、寄什麼，完全由貴司決定。

**Q：執行時會不會影響正在使用軟體的同事？**
A：不會。工具全程唯讀。唯一的例外是使用 `-Feature` 參數時，會短暫取用一張授權來
驗證是否真的能取得授權，驗證完立即歸還；而且工具會先確認該項授權尚有空閒才會測試，
不會搶走最後一張。

**Q：可以重複執行嗎？**
A：可以。每次執行都會產生新的報告檔，不會覆蓋舊的。

**Q：報告要保留多久？**
A：問題解決後即可刪除 `reports` 資料夾內的檔案。

---

虎門科技股份有限公司　Taiwan Auto-Design Co.
技術支援：cae-support@cadmen.com
